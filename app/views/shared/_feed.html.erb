<%= turbo_frame_tag "feed_items" do%>
<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <div class="digg_pagination">
    <%= will_paginate @feed_items %>
  </div>
<% end %>
<% end %>